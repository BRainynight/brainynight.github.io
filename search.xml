<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>部落格指令自動化</title>
    <url>/2021/12/30/automatic-command/</url>
    <content><![CDATA[<p>想用原始一點的方法來搞個半自動化，第一步當然就是不要打那麼多指令！</p>
<p>我希望能將 <code>hexo clean</code>, <code>hexo generate</code>, <code>hexo server</code>/<code>hexo deploy</code> 的步驟集合在一起，並且開啟部落格的 Demo 網址。這裡使用 Make 做指令簡化</p>
<p>在部落格的根目錄創建一個 makefile，內容如下</p>
<span id="more"></span>

<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">local:</span></span><br><span class="line">	python -m webbrowser <span class="string">&quot;http://localhost:4000&quot;</span></span><br><span class="line">	hexo cl &amp;&amp; hexo g &amp;&amp; hexo server </span><br><span class="line"></span><br><span class="line"><span class="section">demo:</span></span><br><span class="line">	python -m webbrowser <span class="string">&quot;http://localhost:4000&quot;</span></span><br><span class="line">	hexo server </span><br><span class="line"></span><br><span class="line"><span class="section">push:</span></span><br><span class="line">	python -m webbrowser <span class="string">&quot;https://&lt;UserName&gt;.github.io/&quot;</span></span><br><span class="line">	hexo cl &amp;&amp; hexo g &amp;&amp; hexo d </span><br></pre></td></tr></table></figure>

<p>需要注意，Command 的縮排是 tab 不是空白鍵！</p>
<h2 id="說明"><a href="#說明" class="headerlink" title="說明"></a>說明</h2><p>先開啟網頁是因為一旦 server 打開就會佔一個 process  在那邊，阻斷後面的指令。而網頁先開啟來只是一個動作，不會阻斷後續的指令。</p>
<h3 id="local"><a href="#local" class="headerlink" title="local"></a><code>local</code></h3><p>是每次改完部落格，要清除靜態檔案重新生成並打 local 的展示網頁的時候。</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">make <span class="built_in">local</span></span><br></pre></td></tr></table></figure>

<h3 id="demo"><a href="#demo" class="headerlink" title="demo"></a><code>demo</code></h3><p>單純想打開 local demo，沒有要清掉資料重新生成。</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">make demo</span><br></pre></td></tr></table></figure>

<h3 id="push"><a href="#push" class="headerlink" title="push"></a><code>push</code></h3><p>確認本地的改動，要將資料 push 到線上了。</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">make push</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>blog</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 視覺化 matplotlib 基本設置</title>
    <url>/2021/12/30/python-visualize-basic/</url>
    <content><![CDATA[<h2 id="子圖"><a href="#子圖" class="headerlink" title="子圖"></a>子圖</h2><h3 id="add-subplot"><a href="#add-subplot" class="headerlink" title="add_subplot"></a><code>add_subplot</code></h3><p>初使用時最常接觸的就是這個方法，當要用某張子圖的時候再使用 <code>fig.add_subplot()</code> 得到某個 <code>ax</code> ，用它進行繪圖。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line">fig = plt.figure(figsize=(<span class="number">20</span>, <span class="number">16</span>), dpi=<span class="number">300</span>) </span><br><span class="line">fig.suptitle(<span class="string">&quot;Big title&quot;</span>, fontsize=<span class="number">20</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 子圖 1</span></span><br><span class="line">ax = fig.add_subplot(<span class="number">2</span>, <span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line">ax.title.set_text(<span class="string">&quot;Subtitle 1&quot;</span>)</span><br><span class="line">plt.plot(np.arange(<span class="number">1</span>, <span class="number">11</span>), color=<span class="string">&#x27;b&#x27;</span>, label=<span class="string">&quot;lr&quot;</span>, alpha=<span class="number">0.7</span>)</span><br><span class="line">ax.legend() <span class="comment"># 圖例</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 子圖 2</span></span><br><span class="line">ax = fig.add_subplot(<span class="number">2</span>, <span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line">ax.title.set_text(<span class="string">&quot;Subtitle 1&quot;</span>)</span><br><span class="line">plt.plot(np.arange(<span class="number">1</span>, <span class="number">11</span>), color=<span class="string">&#x27;r&#x27;</span>, label=<span class="string">&quot;lr&quot;</span>, alpha=<span class="number">0.7</span>)</span><br><span class="line">ax.legend()</span><br><span class="line"></span><br><span class="line">plt.savefig(<span class="string">&quot;fileName.png&quot;</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>但我並不喜歡這個用法，以上面這份程式為例，這總作法會讓 <code>fig.add_subplot(2, 1, 1)</code> 散布在整份程式中，一旦我要修改子圖在行列上的數量，修正起來變得無比麻煩。</p>
<h3 id="一口氣指定完子圖"><a href="#一口氣指定完子圖" class="headerlink" title="一口氣指定完子圖"></a>一口氣指定完子圖</h3><p>一種方法是：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">fig, axes = plt.subplots(n_rows, n_cols)</span><br></pre></td></tr></table></figure>

<p>不錯的是可以一口氣解決，但我更喜歡另一種作法：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">fig = plt.figure(figsize=(<span class="number">10</span>, <span class="number">8</span>), dpi=<span class="number">100</span>)</span><br><span class="line">axes = fig.subplots(<span class="number">2</span>, <span class="number">3</span>).reshape(-<span class="number">1</span>) <span class="comment"># axes.shape: (6, )</span></span><br><span class="line">axes = fig.subplots(<span class="number">2</span>, <span class="number">3</span>)             <span class="comment"># axes.shape: (2, 3)</span></span><br></pre></td></tr></table></figure>

<p>原先的作法在取用 <code>axes</code> 的時候，要謹慎的選取 <code>axes[0][1]</code> 之類的 index ，同樣的一旦子圖在行列上的數量改變，透過 index 取得 <code>ax</code> 的時候也很容易出錯。因此我習慣先把他 <code>reshape</code> 成一個維度，直接用 <code>ax = axes[i]</code> 取用，簡單省事。缺點是難以知道現在處於哪一行哪一列，如果子圖有需要落在確切位置的需求，這個簡單粗暴的方法可能就不太適合。</p>
<h3 id="大標題"><a href="#大標題" class="headerlink" title="大標題"></a>大標題</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ax.set_title(<span class="string">&quot;sub plot title&quot;</span>) <span class="comment"># 子圖的標題</span></span><br><span class="line">fig.suptitle(<span class="string">&quot;Figure title&quot;</span>)   <span class="comment"># 整張圖的大標題</span></span><br></pre></td></tr></table></figure>

<h3 id="合併子圖、跨行跨列"><a href="#合併子圖、跨行跨列" class="headerlink" title="合併子圖、跨行跨列"></a>合併子圖、跨行跨列</h3><h4 id="subplot2grid"><a href="#subplot2grid" class="headerlink" title="subplot2grid"></a><code>subplot2grid</code></h4><p>上面的用法都是規規矩矩，每張子圖大小一致。想要讓圖的形狀跨越多欄多列，應該使用 <code>subplot2grid</code>。<a href="https://matplotlib.org/3.1.1/gallery/userdemo/demo_gridspec01.html#sphx-glr-gallery-userdemo-demo-gridspec01-py" title="matplotlib 官網 subplot2grid 的範例">subplot2grid example</a></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ax1 = plt.subplot2grid((<span class="number">3</span>,<span class="number">3</span>), (<span class="number">0</span>,<span class="number">0</span>), colspan=<span class="number">3</span>)</span><br><span class="line">ax2 = plt.subplot2grid((<span class="number">3</span>,<span class="number">3</span>), (<span class="number">1</span>,<span class="number">0</span>), colspan=<span class="number">2</span>)</span><br><span class="line">ax3 = plt.subplot2grid((<span class="number">3</span>,<span class="number">3</span>), (<span class="number">1</span>, <span class="number">2</span>), rowspan=<span class="number">2</span>)</span><br><span class="line">ax4 = plt.subplot2grid((<span class="number">3</span>,<span class="number">3</span>), (<span class="number">2</span>, <span class="number">0</span>))</span><br><span class="line">ax5 = plt.subplot2grid((<span class="number">3</span>,<span class="number">3</span>), (<span class="number">2</span>, <span class="number">1</span>))</span><br></pre></td></tr></table></figure>

<p>得到的圖相當於：</p>
<p><img src="https://matplotlib.org/3.1.1/_images/sphx_glr_demo_gridspec01_001.png" alt="subplot2grid"></p>
<p>事實上 <code>subplot2grid</code> 依然可以作到像是 <code>subplot</code> 這樣只給出正規方正的子圖，在不做 <code>colspan</code>/<code>rowspan</code> 的情況下，這兩個操作是等價的：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ax = plt.subplot(<span class="number">2</span>,<span class="number">2</span>,<span class="number">1</span>)  <span class="comment"># 1. 算子圖要從 1 開始算子圖</span></span><br><span class="line">ax = plt.subplot2grid((<span class="number">2</span>,<span class="number">2</span>),(<span class="number">0</span>, <span class="number">0</span>)) <span class="comment"># 2. 先規定整張圖的 (row, col), 在列出要回傳的子圖位於哪個位置 (不像上面是算 index)</span></span><br></pre></td></tr></table></figure>

<h4 id="GridSpec"><a href="#GridSpec" class="headerlink" title="GridSpec"></a><code>GridSpec</code></h4><p>還有另一個方法是透過 <code>GridSpec</code> 達成。我們可以先用 <code>gridspec</code> 創建一個框架，並把框架的某一塊指派給 <code>plt.subplot</code> 真正創建子圖。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> matplotlib.gridspec <span class="keyword">as</span> gridspec</span><br><span class="line">gs = gridspec.GridSpec(<span class="number">2</span>, <span class="number">2</span>)</span><br><span class="line">ax = plt.subplot(gs[<span class="number">0</span>, <span class="number">0</span>]) <span class="comment"># 等價 ax = plt.subplot2grid((2,2),(0, 0))</span></span><br></pre></td></tr></table></figure>

<p>所以，在前面一小節的分隔圖，在這個章節應該如下實做：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">gs = gridspec.GridSpec(<span class="number">3</span>, <span class="number">3</span>)</span><br><span class="line">ax1 = plt.subplot(gs[<span class="number">0</span>, :])</span><br><span class="line">ax2 = plt.subplot(gs[<span class="number">1</span>,:-<span class="number">1</span>])</span><br><span class="line">ax3 = plt.subplot(gs[<span class="number">1</span>:, -<span class="number">1</span>])</span><br><span class="line">ax4 = plt.subplot(gs[-<span class="number">1</span>,<span class="number">0</span>])</span><br><span class="line">ax5 = plt.subplot(gs[-<span class="number">1</span>,-<span class="number">2</span>])</span><br></pre></td></tr></table></figure>

<h4 id="subplot2grid-與-GridSpec-的差別"><a href="#subplot2grid-與-GridSpec-的差別" class="headerlink" title="subplot2grid 與 GridSpec 的差別"></a><code>subplot2grid</code> 與 <code>GridSpec</code> 的差別</h4><p>我覺得可這樣看待</p>
<ul>
<li><p><code>subplot2grid</code> 屬於 pyplot 類別中，它直接與畫面操作有關，所以直接指派位置就有圖出來了</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ax = plt.subplot2grid((2,2),(0, 0))</span><br></pre></td></tr></table></figure></li>
<li><p>但是 <code>gridspec</code> 不是屬於 pyplot ，如果只有創建而沒有再指定畫布，是不會出現東西的</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> matplotlib.gridspec <span class="keyword">as</span> gridspec</span><br><span class="line">gs = gridspec.GridSpec(<span class="number">2</span>, <span class="number">2</span>)</span><br><span class="line">plt.show() <span class="comment"># 不會有圖跑出來喔</span></span><br></pre></td></tr></table></figure></li>
</ul>
<p>此外，gridspec 能做到的 layout(布局) 也更加彈性，詳細可以在這篇裡面看: <a href="https://matplotlib.org/2.0.2/users/gridspec.html#customizing-location-of-subplot-using-gridspec">Customizing Location of Subplot Using GridSpec</a> ，中文版: <a href="https://wizardforcel.gitbooks.io/matplotlib-user-guide/content/3.3.html">使用 GridSpec 自定义子图位置</a>。</p>
<p><img src="https://matplotlib.org/2.0.2/_images/demo_gridspec06.png" alt="gridspec 更加彈性的布局"></p>
<h2 id="圖上標示與設置"><a href="#圖上標示與設置" class="headerlink" title="圖上標示與設置"></a>圖上標示與設置</h2><h3 id="限制座標軸數值"><a href="#限制座標軸數值" class="headerlink" title="限制座標軸數值"></a>限制座標軸數值</h3><p>限制 x 與 y 軸顯示的數值上下界。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ax.set_ylim([<span class="number">0</span>, <span class="number">0.1</span>])</span><br><span class="line">ax.set_xlim([<span class="number">0</span>, <span class="number">10</span>])</span><br></pre></td></tr></table></figure>

<h3 id="繪製橫-直線"><a href="#繪製橫-直線" class="headerlink" title="繪製橫/直線"></a>繪製橫/直線</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ax.axhline(y=<span class="number">0.03</span>, color=<span class="string">&#x27;r&#x27;</span>, linestyle=<span class="string">&#x27;-.&#x27;</span>, alpha=<span class="number">0.2</span>)  <span class="comment"># 橫線</span></span><br><span class="line">ax.axvline(x=<span class="number">5</span>, color=<span class="string">&#x27;b&#x27;</span>, linestyle=<span class="string">&#x27;-*&#x27;</span>, alpha=<span class="number">0.2</span>)  <span class="comment"># 直線</span></span><br></pre></td></tr></table></figure>

<h3 id="縮減空白邊緣"><a href="#縮減空白邊緣" class="headerlink" title="縮減空白邊緣"></a><a href="https://stackoverflow.com/questions/55767312/how-to-position-suptitle" title="stackoverflow: How to position suptitle?">縮減空白邊緣</a></h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">fig.tight_layout(rect=[<span class="number">0</span>, <span class="number">0.03</span>, <span class="number">1</span>, <span class="number">0.95</span>]) <span class="comment"># 保留標題的空間</span></span><br><span class="line">fig.suptitle(<span class="string">&quot;title&quot;</span>)</span><br><span class="line">fig.savefig(<span class="string">&quot;fig.png&quot;</span>)</span><br></pre></td></tr></table></figure>

<p><code>tight_layout</code> 要在所有繪圖都結束之後，就要 <code>savefig</code> 或是 <code>plt.show()</code> 之前再放才會生效！或是在 import之後直接改變全局：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line">plt.rcParams[<span class="string">&quot;figure.autolayout&quot;</span>] = <span class="literal">True</span></span><br></pre></td></tr></table></figure>

<h2 id="全局的設置-matplotlibrc"><a href="#全局的設置-matplotlibrc" class="headerlink" title="全局的設置 matplotlibrc"></a><a href="https://matplotlib.org/stable/tutorials/introductory/customizing.html?highlight=figure.autolayout#the-matplotlibrc-file" title="matplotlib 官網 subplot2grid 的 matplotlibrc 設置">全局的設置 <code>matplotlibrc</code></a></h2><p><code>matplotlib</code> 裡面有很多設定，包含 <code>dpi</code>, <code>figsize</code>, <code>color</code>, <code>linewidth</code>…..，如果希望一些改變能夠直接作用在 global 上，可以透過設定 <code>matplotlibrc</code> 檔案達成：</p>
<p>在沒有使用 <code>style.use(&#39;&lt;path&gt;/&lt;style-name&gt;.mplstyle&#39;)</code> 指向設定檔時，預設的搜索順序是這樣：</p>
<ol>
<li>當前資料夾：當前這個資料夾中存在 <code>matplotlibrc</code>，而在這個資料夾之中所有的繪圖，也都想用這樣的設定套用，那非常適合這麼做。</li>
<li>如果存在環境變數 <code>$MATPLOTLIBRC</code>：如果這是個檔案，就直接用。如果是個資料夾，會尋找並使用其下的 <code>$MATPLOTLIBRC/matplotlibrc</code> </li>
<li>恩，剩下兩條我覺得太麻煩了，懶得翻譯。可以去看原文。</li>
</ol>
<p>如果上述的路徑下都沒有  <code>matplotlibrc</code> 檔案那不會再額外搜索其他地方。我們可以直接透過下面這個的作法尋找當前使用的是誰：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> matplotlib</span><br><span class="line">matplotlib.matplotlib_fname()</span><br></pre></td></tr></table></figure>

<h4 id="我的-matplotlibrc-檔案"><a href="#我的-matplotlibrc-檔案" class="headerlink" title="我的 matplotlibrc 檔案"></a>我的 <code>matplotlibrc</code> 檔案</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">figure.autolayout: True</span><br><span class="line">legend.fontsize:   large</span><br><span class="line">figure.titlesize:   x-large</span><br><span class="line">figure.dpi:  100</span><br><span class="line">axes.titlesize:     x-large </span><br><span class="line">image.cmap: hsv</span><br><span class="line">hist.bins: 20 </span><br></pre></td></tr></table></figure>



<hr>
]]></content>
      <tags>
        <tag>python</tag>
        <tag>visualize</tag>
      </tags>
  </entry>
  <entry>
    <title>新年快樂之部落格沿革(?)</title>
    <url>/2022/01/01/blog-source/</url>
    <content><![CDATA[<p>2022 新年快樂！新年的第一篇文章，來寫個段小故事。</p>
<p>我相信自己一路走來在各種平台、格式上轉換的痛苦已經算少了，但每次想起依然常常覺得…. 小坎坷阿，希望現在的決定夠有遠見了？</p>
<span id="more"></span>

<h3 id="純樸的文字編輯器：Evernote"><a href="#純樸的文字編輯器：Evernote" class="headerlink" title="純樸的文字編輯器：Evernote"></a>純樸的文字編輯器：Evernote</h3><p>從高中起一直有寫心得、觀後感與日記的習慣，那時候用的是 Evernote。大學開始寫程式時，也有著紀錄 bug 的習慣，那時候用的是 Line 記事本跟 Evernote。可以想見，這種紀錄方式簡直是災難… 我希望至少 Code 可以是彩色的（好卑微阿），開始尋找替代方案。</p>
<p>儘管將程式相關的內容從 Evernote 抽離，它依然是我挺喜歡的筆記軟體。但，隨著 Markdown 語法越來越盛行，Evernote 卻遲遲不能夠跟進，加上 Notion 後來出了教育帳戶免費的策略，最後包括慣用的其他紀錄也都轉移到 Notion 去了。</p>
<h3 id="Blogger"><a href="#Blogger" class="headerlink" title="Blogger"></a>Blogger</h3><p>由於當時有在用 Blogger 在寫遊記，所以第一時間想到的替代工具就是 Google 旗下的 Blogger ，大約也用了一年多。當對排版越來越要求，發現 Blogger 那種 word 編輯器 + html 編輯界面的方式實在是太沒效率了！決定再次轉移。</p>
<h3 id="HackMD"><a href="#HackMD" class="headerlink" title="HackMD"></a>HackMD</h3><p>開始用 Markdown 在寫技術筆記，並使用 HackMD 作為平台。但 HackMD 有個很大的缺點，它不支援內文搜尋模式….. （曾經支援，後來疑似因為 loading 太大拿掉內文搜索只能找標題）文章一多不記得什麼內容在哪篇文章，這根本災難！只好又開始尋找替代方案</p>
<p>那個時期不知道為什麼，HackMD 老是不溫不火，號稱是從日本 IT 圈紅回來的軟體，在台灣小有名氣其實也很好用。但感覺一直是維持狀態，更新狀態不大，反而有退一步取消內文搜索功能的這種事件。</p>
<h3 id="Github-Typora"><a href="#Github-Typora" class="headerlink" title="Github + Typora"></a>Github + Typora</h3><p>腦子動到 Github 的頭上，發展出用離線的 Markdown 編輯器 Typora 加上 Github 做同步。在我開始將 HackMD 的內容拉下來，跟本地的離線檔案整併時發現有些筆記不小心使用到了一些只有 HackMD 支援的 Markdown 延伸語法，導致這些內容在 Typora 上完全沒有效果！！像是亂碼一樣！</p>
<p>至此，我下定決心，以後寫文章絕對不要再搞出這麼多麻煩，乖乖用基礎的 Markdown 語法，很方便了！別搞那麼多事！</p>
<h3 id="熱門的部落格平台：Medium"><a href="#熱門的部落格平台：Medium" class="headerlink" title="熱門的部落格平台：Medium"></a>熱門的部落格平台：Medium</h3><p>想有個部落格的心情依舊在，我曾嘗試過 Medium 平台並不是很喜歡。一來是平台的彈性低、程式碼甚至沒辦法上色，非得透過 Gist ；二來是 Medium 還是比較適合英文文章，英文字寫起來格式是又優雅又漂亮，但中文一個斜體就搞得版面很不和諧。後來也就拿來放一些跟 Code 無關的事情，一些搞環境經驗之類的而已。</p>
<h3 id="初體驗靜態網頁產生器：Pelican"><a href="#初體驗靜態網頁產生器：Pelican" class="headerlink" title="初體驗靜態網頁產生器：Pelican"></a>初體驗靜態網頁產生器：Pelican</h3><p>大四時一度嘗試使用 Github Page + Pelican 做靜態網站生成，挑選這個組合是因為覺得綁定網域得付錢、感覺這部落格又是有一更沒一更不知道哪天會不會荒廢，決定先從免費的試試看，Github Page 是這個選項很好的解答。而 Pelican 是它背後是用 Python 寫的，好歹我比較熟 (不敢一下子踏入 js) 。</p>
<p>不知道是對網站管理沒經驗、放太多圖片塞到流量還是什麼問題，大約半年覺得網站載入太慢了受不了，加上對版面的主題既不滿意、又改不動，在充滿羞恥的心情下收掉了這個第一個自己架的部落格。</p>
<h3 id="再次回歸-Github-Typora"><a href="#再次回歸-Github-Typora" class="headerlink" title="再次回歸 Github + Typora"></a>再次回歸 Github + Typora</h3><p>此後，我一直用 Typora + Github 的模式寫筆記，由於有過自己搭建的經驗，每次看到別人的部落格是使用 Github page 的時候，都會多留意兩眼框架與主題，以及一些自己喜歡的小工具、要素等等，也是那時候看到 Hexo 與 Next 主題被很多人採用。過去使用 Peclian 時，我極度討厭文章太長，每次都要卷到最上面才能點目錄跳轉的情況，甚至自己研究用 CSS 弄了一個懸浮目錄，功能是有卻醜到自己受不了，關站很大一個原因也是這樣。</p>
<h3 id="現在：Hexo"><a href="#現在：Hexo" class="headerlink" title="現在：Hexo"></a>現在：Hexo</h3><p>直到最近看到有在追蹤的一位作者的文章<a href="https://www.ithome.com.tw/voice/148476">技術網站架設經驗雜談</a>，邊看產生了很多共鳴，想到了一直有個小小的期許：能彈性控制的部落格！才鼓起勇氣再次搭建了部落格。這次打算嘗試許多人都採用的 Hexo 來搭建，管他語言不一樣，大不了學學吧！於是最終就有了這個部落格。</p>
<p>恩，真是一段不長不短的歷史。總之就是告誡自己，不要被漂亮的擴充功能迷惑，換了框架很痛苦的。</p>
]]></content>
      <tags>
        <tag>blog</tag>
        <tag>about</tag>
      </tags>
  </entry>
  <entry>
    <title>建置 Hexo 部落格</title>
    <url>/2021/12/30/hexo-build-env/</url>
    <content><![CDATA[<h2 id="前置作業"><a href="#前置作業" class="headerlink" title="前置作業"></a>前置作業</h2><ol>
<li><p>安裝 <a href="https://nodejs.org/en/">Node.js</a> ，下載安裝檔後一路按到底，安裝完成。</p>
</li>
<li><p>查看是否安裝成功：打開 CMD，輸入</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">node -v</span><br><span class="line">npm -v</span><br></pre></td></tr></table></figure>

<p>應該會顯示已經安裝的版本。</p>
</li>
<li><p>安裝 Hexo：打開 CMD 輸入</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">npm install hexo-cli -g</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">added 57 packages, and audited 58 packages in 8s</span><br><span class="line"></span><br><span class="line">11 packages are looking for funding</span><br><span class="line">  run `npm fund` for details</span><br><span class="line"></span><br><span class="line">found 0 vulnerabilities</span><br><span class="line">npm notice</span><br><span class="line">npm notice New minor version of npm available! 8.1.2 -&gt; 8.3.0</span><br><span class="line">npm notice Changelog: https://github.com/npm/cli/releases/tag/v8.3.0</span><br><span class="line">npm notice Run npm install -g npm@8.3.0 to update!</span><br><span class="line">npm notice</span><br></pre></td></tr></table></figure></li>
<li><p>查看 Hexo 是否安裝成功：看下面的指令有沒有成功顯示一些相關資訊</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">hexo version </span><br></pre></td></tr></table></figure></li>
</ol>
<p>全部前置作業完成！</p>
<h2 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h2><ol>
<li><p>建立一個資料夾，這裡創立 <code>blog-hexo</code></p>
</li>
<li><p>進入資料夾，下指令：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">hexo init . </span><br></pre></td></tr></table></figure></li>
<li><p>（仍在資料夾中）下指令安裝需要的 npm 套件</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">npm install</span><br></pre></td></tr></table></figure>

<p>不需要安裝會出現象這樣的訊息：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">up to date, audited 249 packages <span class="keyword">in</span> 976ms</span><br><span class="line"></span><br><span class="line">15 packages are looking <span class="keyword">for</span> funding</span><br><span class="line">  run `npm fund` <span class="keyword">for</span> details</span><br><span class="line"></span><br><span class="line">1 moderate severity vulnerability</span><br><span class="line"></span><br><span class="line">To address all issues (including breaking changes), run:</span><br><span class="line">  npm audit fix --force</span><br><span class="line"></span><br><span class="line">Run `npm audit` <span class="keyword">for</span> details.</span><br></pre></td></tr></table></figure></li>
<li><p>可以嘗試 Demo 預設的網頁：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">hexo server</span><br></pre></td></tr></table></figure></li>
</ol>
<h2 id="Hexo-設定檔"><a href="#Hexo-設定檔" class="headerlink" title="Hexo 設定檔"></a>Hexo 設定檔</h2><p>現在我們回到 <code>blog-hexo</code> 資料夾中，注意到 <code>_config.yml</code> 設定檔。</p>
<ol>
<li><p>其中 <code>theme</code> 參數是有關主題的參數，將它設置為想要的主題，這裡我想採用 <code>next</code></p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Extensions</span></span><br><span class="line"><span class="comment">## Plugins: https://hexo.io/plugins/</span></span><br><span class="line"><span class="comment">## Themes: https://hexo.io/themes/</span></span><br><span class="line"><span class="attr">theme:</span> <span class="string">next</span> </span><br></pre></td></tr></table></figure></li>
<li><p>到<code>theme</code>資料夾，將主題的 github 專案 clone 下來</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/theme-next/hexo-theme-next.git next</span><br></pre></td></tr></table></figure></li>
</ol>
<h3 id="改地區"><a href="#改地區" class="headerlink" title="改地區"></a>改地區</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Site</span></span><br><span class="line"><span class="attr">title:</span> <span class="string">Blog</span> <span class="string">Title</span></span><br><span class="line"><span class="attr">subtitle:</span> <span class="string">&#x27;&#x27;</span></span><br><span class="line"><span class="attr">description:</span> <span class="string">&#x27;&#x27;</span></span><br><span class="line"><span class="attr">keywords:</span> <span class="comment"># 可以下一些對部落格的關鍵字，方便搜尋引擎找到</span></span><br><span class="line"><span class="attr">author:</span> <span class="string">Who</span></span><br><span class="line"><span class="attr">language:</span> <span class="string">zh-TW</span> <span class="comment"># 改成 zh-TW, 大寫！不然在 hexo generate 時會變成阿拉伯文...</span></span><br><span class="line"><span class="attr">timezone:</span> <span class="string">&#x27;&#x27;</span></span><br></pre></td></tr></table></figure>

<h3 id="Github-部屬相關"><a href="#Github-部屬相關" class="headerlink" title="Github 部屬相關"></a>Github 部屬相關</h3><p>執行這個步驟前需要先在 Github 上創建倉庫，參見：[上傳到 Github 倉庫](#上傳到 Github 倉庫)。預設的情況通常倉庫名稱會建置為 <code>username.github.io</code>，以下就以預設的情況舉例說明。</p>
<ol>
<li><p>部落格網址：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment">## Set your site url here. For example, if you use GitHub Page, set url as &#x27;https://username.github.io/project&#x27;</span></span><br><span class="line"><span class="attr">url:</span> <span class="string">https://&lt;使用者名稱&gt;.github.io/</span></span><br></pre></td></tr></table></figure></li>
<li><p>使用 <code>hexo deploy</code> 到 Github 上面的參數選項：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">  <span class="attr">repo:</span> <span class="string">https://github.com/username/username.github.io.git</span> <span class="comment"># http git clone 所給的網址</span></span><br><span class="line">  <span class="attr">branch:</span> <span class="string">master</span> <span class="comment"># 或 main, 看主要的 branch 名稱是什麼 </span></span><br></pre></td></tr></table></figure>

<p><code>branch</code> 如果是由本地端使用 <code>git init .</code> 創建，預設的分支名稱會是 <code>master</code> 。但如果是在 github 倉庫上面創建先的話，預設會是 <code>main</code>。Hexo 可以部屬到很多地方，如果在這邊用了多個 <code>type</code>，可以在使用 deploy 時一口氣部屬到多個網站：<a href="https://hexo.io/zh-tw/docs/one-command-deployment.html">Deployment | Hexo</a></p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">deploy:</span></span><br><span class="line"><span class="bullet">-</span> <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">  <span class="attr">repo:</span></span><br><span class="line"><span class="bullet">-</span> <span class="attr">type:</span> <span class="string">heroku</span></span><br><span class="line">  <span class="attr">repo:</span></span><br></pre></td></tr></table></figure></li>
</ol>
<h2 id="主題設定檔"><a href="#主題設定檔" class="headerlink" title="主題設定檔"></a>主題設定檔</h2><h3 id="Scheme"><a href="#Scheme" class="headerlink" title="Scheme"></a>Scheme</h3><p><code>Next</code> 主題中還有不同的排版模式，設定要進入 <code>theme/next/_config.yml</code> 修改：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">scheme:</span> <span class="string">Gemini</span></span><br></pre></td></tr></table></figure>

<h3 id="Menu-要顯示的內容"><a href="#Menu-要顯示的內容" class="headerlink" title="Menu 要顯示的內容"></a>Menu 要顯示的內容</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">menu:</span></span><br><span class="line">  <span class="attr">home:</span> <span class="string">/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-home</span></span><br><span class="line">  <span class="attr">about:</span> <span class="string">/about/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-user</span></span><br><span class="line">  <span class="attr">tags:</span> <span class="string">/tags/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-tags</span></span><br><span class="line">  <span class="comment">#categories: /categories/ || fa fa-th</span></span><br><span class="line">  <span class="attr">archives:</span> <span class="string">/archives/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-archive</span></span><br><span class="line">  <span class="comment">#schedule: /schedule/ || fa fa-calendar</span></span><br><span class="line">  <span class="comment">#sitemap: /sitemap.xml || fa fa-sitemap</span></span><br><span class="line">  <span class="comment">#commonweal: /404/ || fa fa-heartbeat</span></span><br></pre></td></tr></table></figure>

<p>影響的是左邊 Menu 中顯示的分頁內容，預設只有 home 跟 archives，其他項目如果開起來的話預設的頁面(page) 還沒建立，會連不到內容。</p>
<h3 id="添加客製化-Custom-的設定檔"><a href="#添加客製化-Custom-的設定檔" class="headerlink" title="添加客製化(Custom) 的設定檔"></a><a href="https://oscarcx.com/tech/hexo-customize.html#%E5%85%B6%E5%AE%83%E6%94%B9%E5%8A%A8">添加客製化(Custom) 的設定檔</a></h3><p>將想添加的項目取消註解，這裡最簡單粗暴的只使用 <code>style</code>。寫在 <code>styles.styl</code> 中的 css 會被添加到最尾端，因此可以直接覆蓋默認的格式。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">custom_file_path:</span></span><br><span class="line">  <span class="comment">#head: source/_data/head.swig</span></span><br><span class="line">  <span class="comment">#header: source/_data/header.swig</span></span><br><span class="line">  <span class="comment">#sidebar: source/_data/sidebar.swig</span></span><br><span class="line">  <span class="comment">#postMeta: source/_data/post-meta.swig</span></span><br><span class="line">  <span class="comment">#postBodyEnd: source/_data/post-body-end.swig</span></span><br><span class="line">  <span class="comment">#footer: source/_data/footer.swig</span></span><br><span class="line">  <span class="comment">#bodyEnd: source/_data/body-end.swig</span></span><br><span class="line">  <span class="comment">#variable: source/_data/variables.styl</span></span><br><span class="line">  <span class="comment">#mixin: source/_data/mixins.styl</span></span><br><span class="line">  <span class="attr">style:</span> <span class="string">source/_data/styles.styl</span></span><br></pre></td></tr></table></figure>



<h2 id="第一篇文章"><a href="#第一篇文章" class="headerlink" title="第一篇文章"></a><a href="https://hexo.io/zh-tw/docs/writing">第一篇文章</a></h2><ol>
<li><p>執行下述指令，Hexo 會根據 Layout 的種類建立一個 markdown 檔案在對應的位置，Layout 預設為 Post。下面的指令就會在 <code>source/_posts</code> 下建立 <code>NewPost</code></p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">hexo new [Layout] &lt;PageName&gt;</span><br><span class="line">hexo new <span class="string">&quot;NewPost&quot;</span></span><br></pre></td></tr></table></figure>

<p><code>source</code> 中是存放各種頁面 markdown 位置，<code>public</code> 中則是存已經渲染成 html 格式的檔案。</p>
<p><a href="https://hexo.io/zh-tw/docs/writing#%E4%BD%88%E5%B1%80%EF%BC%88Layout%EF%BC%89">Layout 的種類有三種</a>：</p>
<ul>
<li><p><code>post </code>：預設種類，儲存的位置是 <code>source/_posts</code></p>
</li>
<li><p><code>page</code>：路徑是 <code>source</code>，用途應該像是上面 Menu 環節中的 <code>Tags</code>, <code>About</code> 頁面需要創建時，就該輸入 <code>hexo new page Tags</code> 之類的吧。</p>
</li>
<li><p><code>draft</code>: 儲存路徑是 <code>source/_drafts</code> ，存在這裡的檔案不會顯示在頁面上。當要發布時，可以透過 </p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">hexo publish [layout] &lt;draft_title&gt;</span><br></pre></td></tr></table></figure>

<p>將草稿從 <code>_drafts</code> 挪到 <code>_posts</code> 或變成 <code>page</code> 之類的。</p>
</li>
</ul>
</li>
<li><p>來到 <code>source/_posts/NewPost.md</code> (應該還可以看到同層級還有一個 Hello world) ，預設的內容如下，紀錄了一些文章的 Metadata。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: NewPost</span><br><span class="line">date: 2021-12-26 22:40:55</span><br><span class="line"><span class="section">tags: tag1 tag2</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line"><span class="section"># 標題一</span></span><br><span class="line">寫一些東西吧！</span><br></pre></td></tr></table></figure></li>
<li><p>到根目錄 <code>source</code> 上：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">hexo generate <span class="comment"># 產生靜態 page </span></span><br><span class="line">hexo server <span class="comment"># Demo 在 localhost:4000 上</span></span><br></pre></td></tr></table></figure></li>
</ol>
<h3 id="scaffolds"><a href="#scaffolds" class="headerlink" title="scaffolds"></a>scaffolds</h3><p>在創建文章的時候，根據 <code>layout</code> 種類，Hexo 會生成一個對應名稱的 .md 檔案在指定的路徑內。而這個檔案通常會有一些預設的內容，例如：<code>post</code> 格式就會有上一個章節中所展示的，有著預設的 metadata：<code>title</code>, <code>date</code>, <code>tags</code> 三個項目。</p>
<p>如果想添加更多的屬性在預設產生的模板中，可以在 <code>scaffolds/post.md</code> 中添加。預設中的形式如下：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: &#123;&#123; title &#125;&#125;</span><br><span class="line">date: &#123;&#123; date &#125;&#125;</span><br><span class="line"><span class="section">tags:</span></span><br><span class="line"><span class="section">---</span></span><br></pre></td></tr></table></figure>

<h3 id="創造頁面-page"><a href="#創造頁面-page" class="headerlink" title="創造頁面(page)"></a>創造頁面(page)</h3><p>我們在[前面](#Menu 要顯示的內容)選擇了 Menu 裡面要顯示什麼，但如果取消註解了而沒有創建對應的頁面，在主頁上會顯示、但連進去之後會是 404。我們需要依照有列出的 page 創建對應的頁面：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">hexo new page categories</span><br><span class="line">hexo new page tags</span><br></pre></td></tr></table></figure>

<p>之類的。參見： <a href="https://kemushi54.github.io/2019/04/01/%E7%AD%86%E8%A8%98-%E6%89%93%E9%80%A0%E8%87%AA%E5%B7%B1%E7%9A%84-blog%EF%BC%8CHexo-Github-%E4%B9%8B%E4%BA%8C/">[筆記] 打造自己的 blog，Hexo + Github 之二</a></p>
<h2 id="上傳-Github-與部屬網站"><a href="#上傳-Github-與部屬網站" class="headerlink" title="上傳 Github 與部屬網站"></a>上傳 Github 與部屬網站</h2><h3 id="上傳到-Github-倉庫"><a href="#上傳到-Github-倉庫" class="headerlink" title="上傳到 Github 倉庫"></a>上傳到 Github 倉庫</h3><ol>
<li><p>在 Github 創建一個名為  <code>&lt;UserName&gt;.github.io</code> 的倉庫，如果已經有過這個名稱，需要使用別的名稱創建倉庫名，可以參考這篇文章：<a href="https://ed521.github.io/2019/07/hexo-install/#gt-%E8%A8%AD%E5%AE%9A%E9%83%A8%E7%BD%B2%E8%87%B3-GitHub-%E7%9A%84%E8%B3%87%E8%A8%8A">[教學]使用 GitHub Pages + Hexo 來架設個人部落格</a></p>
</li>
<li><p>安裝插件</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>

<p>沒安裝的話會出現 <code>ERROR Deployer not found: git</code></p>
</li>
<li><p>在部落格資料夾中開啟 cmd，確認位置是在根目錄下（這篇的例子就是 <code>blog-hexo</code>）</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">hexo cl <span class="comment"># hexo clean 會把 public 內容刪掉</span></span><br><span class="line">hexo g <span class="comment"># hexo generate 建立靜態檔案</span></span><br><span class="line">hexo d <span class="comment"># hexo deploy 部屬到 _config 中有設置的地方</span></span><br></pre></td></tr></table></figure></li>
</ol>
<p>我的電腦因為本來就有跟自己的 github 倉庫做 ssh 連結，所以沒有跳出帳密驗證（還是過去已經有驗證過了但我忘記了？）。但看起來有些人在第一次部屬的時候會需要在跳出視窗登帳號。</p>
<h3 id="Hexo-Deployer-的運作原理"><a href="#Hexo-Deployer-的運作原理" class="headerlink" title="Hexo Deployer 的運作原理"></a><a href="https://github.com/hexojs/hexo-deployer-git#how-it-works">Hexo Deployer 的運作原理</a></h3><p>事實上，<code>hexo-deployer-git</code> 是將網站的靜態檔案產生在 <code>.deploy_git</code> 裡面，並使用強制推送(force pushing) 把東西推上  <code>&lt;UserName&gt;.github.io</code>。也因此，如果我們在根目錄或 <code>public</code> 資料夾再怎麼使用指令</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">git remote -v 	  <span class="comment"># 列出所有遠端伺服器</span></span><br><span class="line">git <span class="built_in">log</span> --oneline <span class="comment"># git commit 紀錄</span></span><br></pre></td></tr></table></figure>

<p>都不會看到什麼內容，但如果進去 <code>.deploy_git</code> 資料夾，一切都不同了。</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">log</span> --oneline</span><br><span class="line">0c468df (HEAD -&gt; master) update blog</span><br><span class="line">7c6b6b6 First commit</span><br></pre></td></tr></table></figure>

<p>但它的 git remote 清單裡面還是空的，我也還沒搞懂運作原理（汗顏</p>
<h3 id="保存當前的內容"><a href="#保存當前的內容" class="headerlink" title="保存當前的內容"></a>保存當前的內容</h3><p>成功部屬後，進入  <code>&lt;UserName&gt;.github.io</code> 的倉庫，可以發現這個倉庫的內容基本上就是 <code>public</code> 資料夾的內容。並且我們可以在本地部落格中，透過 <code>git remote -v</code> 查看是否有遠端倉庫。（此用法參見：<a href="https://git-scm.com/book/zh-tw/v2/Git-%E5%9F%BA%E7%A4%8E-%E8%88%87%E9%81%A0%E7%AB%AF%E5%8D%94%E5%90%8C%E5%B7%A5%E4%BD%9C">Git-基礎-與遠端協同工作</a> ）我這邊看起來是空蕩蕩，什麼都沒有。我希望在不同的平台上都可以撰寫部落格，想把當前的內容備份該如何？</p>
<p>Hexo 在幫我們初始化資料夾時已經有了一個 <code>.gititnore</code> 檔案，裡面已經將他們為了部屬而產生的資料夾給排除，也把 localhsot 在跑得時候所需要的 <code>public</code> 資料夾排除</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.DS_Store</span><br><span class="line">Thumbs.db</span><br><span class="line">db.json</span><br><span class="line">*.log</span><br><span class="line">node_modules/</span><br><span class="line">public/</span><br><span class="line">.deploy*/</span><br></pre></td></tr></table></figure>

<p>所以我們可以不用額外做設定，直接 <code>git add .</code> 把東西加進去 git 倉庫，然後在 github 上面創一個私人的倉庫、按照教學把 github 倉庫跟本地倉庫連結，內容推上去就可以了。</p>
<h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ul>
<li><a href="https://hackmd.io/@Heidi-Liu/note-hexo-github#Hexo-%E7%92%B0%E5%A2%83%E5%BB%BA%E7%BD%AE">【學習筆記】如何使用 Hexo + GitHub Pages 架設個人網誌</a>：介紹如何建制、Hexo 常用的指令、如何發布到 Github 上面。</li>
<li><a href="https://isdaniel.github.io/hexo-blog-theme/">建立自己Blog系列(三) Hexo next theme 介紹</a>：有更詳細的介紹 Configure 區域對應的用途</li>
<li><a href="https://tiida54.github.io/2018/01/05/3%E5%88%86%E9%90%98%E5%AE%8C%E6%88%90HexoBlog%E4%B8%BB%E9%A1%8CNext%E8%A8%AD%E5%AE%9A/">3分鐘完成HexoBlog主題Next設定</a>：講解到 <code>scheme</code> 的部份，但似乎版本有點舊，有些屬性沒找到。</li>
<li><a href="https://ed521.github.io/2019/07/hexo-install/#gt-%E8%A8%AD%E5%AE%9A%E9%83%A8%E7%BD%B2%E8%87%B3-GitHub-%E7%9A%84%E8%B3%87%E8%A8%8A">[教學]使用 GitHub Pages + Hexo 來架設個人部落格</a>：也是屬於從頭部屬 Hexo 並上傳 Github，但在 Github Page 的部份有更仔細的解釋。</li>
<li><a href="https://zenreal.github.io/posts/44730/">雲沐居: 最新 Hexo NexT v7.4.1 主題優化</a>：比較多是對文章內容格式有影響的介紹，像是用一些特殊的 block，螢光筆色這種。</li>
<li><a href="https://oscarcx.com/tech/hexo-customize.html#%E9%AD%94%E6%94%B9footer">Hexo博客个性化配置</a>：<code>Next</code> 主題的進階修改，偏向一些部落格整體的小工具，如閱讀進度條、音樂播放器…。</li>
<li><a href="https://blog.csdn.net/dpdpdppp/article/details/102387532">hexo(Next主题)修改文字大小</a></li>
<li>主題: <a href="https://theme-next.js.org/next-8-8-2-released/#%F0%9F%9B%A0-Improvements">Next</a></li>
<li>主題: <a href="https://github.com/liuyib/hexo-theme-stun/">Stun</a></li>
</ul>
]]></content>
      <tags>
        <tag>blog</tag>
        <tag>hexo</tag>
      </tags>
  </entry>
</search>
